import React from "react";

const ShopCreateProduct = () => {
    return (
        <div>
            <h1>Create new product (only shop)</h1>
        </div>
    );
};

export default ShopCreateProduct;
